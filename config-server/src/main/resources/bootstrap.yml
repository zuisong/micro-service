server:
  port: 8888

spring:
  application:
    name: ConfigServer
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config-dir
  profiles:
    active: native
